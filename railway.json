{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python3 -m pip install -r requirements.txt && npm ci && npm run build && python3 manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "python3 manage.py migrate && gunicorn medicalAssistant.wsgi:application --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
